## 1. Draft the prose in `src/pages/blog/parallelism-roofline.mdx`

- [x] 1.1 Locate the "## DP — One all-reduce at the end" subsection (around `src/pages/blog/parallelism-roofline.mdx:136`) and identify the insertion point: immediately after `<DpBackward client:visible />` and before the existing paragraph that begins "This is also where DP's only compute/communication overlap lives".
- [x] 1.2 Write a one-sentence framing line that introduces a two-layer setup $X \to a_1 \to Y$ scoped to this subsection, while keeping the post's $\partial W$ gradient notation.
- [x] 1.3 Write the "The Dependency Chain" enumeration with the four backprop steps: (a) $\delta_2$, (b) $\partial W_2 = a_1^{\top} \delta_2$ (all-reduced across DP workers), (c) $\delta_1 = (\delta_2 \, W_2^{\top}) \odot \sigma'(z_1)$ (local-only), (d) $\partial W_1 = X^{\top} \delta_1$. Use inline `$...$` LaTeX matching the post's existing math style.
- [x] 1.4 Add the one-sentence callout that Step 3 has no dependency on Step 2's all-reduce, and that this independence is what lets the GPU's compute engine (Tensor Cores) and communication engine (NIC / NCCL / copy engines) run in parallel.
- [x] 1.5 Write the "How the Overlap Works" three-item enumeration: (1) local compute of $\partial W_2$, (2) fire-and-forget async all-reduce of $\partial W_2$ over NCCL / NVLink / InfiniBand, (3) simultaneous layer-1 backward (backward GEMM $\delta_2 \, W_2^{\top}$, element-wise $\sigma'(z_1)$, weight-gradient GEMM $X^{\top} \delta_1$). Name the responsible engine in each item.
- [x] 1.6 Add a closing sentence that ties the schedule to the `<OverlapTimeline strategy="dp" client:visible />` immediately below — explicitly framing the timeline as the layer-by-layer repetition of this schedule. Do NOT edit the existing paragraph or the `<OverlapTimeline />` component itself.
- [x] 1.7 Inline-define each new symbol on first use within the subsection: $a_1$ (activation between layers), $z_1$ (pre-activation in layer 1), $\delta_\ell$ (gradient flowing into layer $\ell$'s pre-activation), $\sigma'$ (activation derivative), $\odot$ (element-wise multiply).

## 2. Verify scoping and consistency

- [x] 2.1 Confirm via `git diff src/pages/blog/parallelism-roofline.mdx` that the only edits are inside the "## DP — One all-reduce at the end" subsection — no changes to FSDP / TP / FSDP+TP backward subsections, the unified-picture table, the appendix, or any imports. *Note: the file already had pre-existing modifications from a prior session affecting the intro, DP, FSDP, TP, FSDP+TP forward sections, and the Backward intro; the new diff from this change is solely the dependency-chain + overlap-walkthrough insertion in the DP backward subsection (lines 142–162).*
- [x] 2.2 Grep the file for the new symbols (`W_1`, `W_2`, `a_1`, `z_1`, `delta_1`, `delta_2`, `\\sigma`, `\\odot`) and confirm every match falls inside the DP backward subsection. *All matches on lines 144–159, inside the subsection that runs from line 136 to line 161.*
- [x] 2.3 Confirm no MDX component imports are added or removed at the top of the file. *Verified — `git diff | grep '^[+-]import'` returns no hits.*

## 3. Visual verification

- [x] 3.1 Run the dev server (`npm run dev` or the project-standard equivalent) and navigate to the parallelism-roofline blog post in a browser. *Astro dev server served the post at http://localhost:4322/blog/parallelism-roofline.*
- [x] 3.2 Scroll to the "DP — One all-reduce at the end" subsection and visually confirm: (a) `<DpBackward />` renders unchanged, (b) the new dependency-chain enumeration renders with all four formulas correctly typeset by KaTeX (in particular $\odot$, $\sigma'(z_1)$, $a_1^{\top} \delta_2$, $\delta_2 \, W_2^{\top}$), (c) the three-item overlap schedule renders below it, (d) the existing overlap paragraph and `<OverlapTimeline strategy="dp" />` follow unchanged. *Confirmed via screenshots: dp-dependency-chain.png, dp-overlap-walkthrough.png, dp-overlap-timeline-after.png.*
- [x] 3.3 Open the browser dev console on this page and confirm zero new console errors or KaTeX parse warnings introduced by the edit. *Playwright `browser_console_messages` at level=error returned 0 errors (3 total messages, 0 errors, 0 warnings).*
- [x] 3.4 Spot-check that the rest of the post (FSDP backward, TP backward, FSDP+TP backward, unified-picture table, appendix) still renders identically — quick scroll, no regressions. *Spot-checked FSDP+TP backward section + unified-picture table — heading and 5-row table both present and rendering normally (see spot-check-fsdptp.png).*
